Important correction: The dashboard must NOT generate extra CSV data.

Currently the app is showing 2,212 rows of generated 5-minute data, but my real CSV file only contains the actual logged records. The website must display ONLY the rows that exist inside the uploaded CSV files.

Please fix the data handling:

1. Remove or disable any fake/sample CSV generation.
2. Do not create 7 days of generated data.
3. Do not create 5-minute dummy records.
4. Use only the uploaded CSV files as the source of truth.
5. The number of rows shown in the dashboard must exactly match the number of rows inside each CSV file.
6. If a CSV file has only 5 records, the sensor page and table must show only 5 records.
7. The Home sensor card record count must show the real CSV row count only.
8. The latest record must be taken from the last row in the CSV file.
9. Keep reading CSV headers dynamically.
10. Do not change the CSV column names.
11. Do not overwrite my uploaded CSV files.
12. Excel export must export only the real filtered CSV rows.

Also fix the sampling interval options. They must be exactly:
- Every 15 minutes = 15M
- Every 30 minutes = 30M
- Every 45 minutes = 45M
- Every 1 hour = 1H
- Every 2 hours = 2H
- Every 3 hours = 3H
- Every 4 hours = 4H
- Every 1 day = 1D

Remove the 5-minute interval option.

Now update the UI design with the following changes:

LOGIN PAGE
1. Change title from:
Weather Station
to:
Smart Weather Station Dashboard Login

2. Add company logo support.
I will attach the company logo later, so create a placeholder image path:
frontend/public/company-logo.png

3. The login top icon should be colorful, not plain.
Use a gradient icon background with blue/cyan/green colors.

4. Make the login background blue with animated soft gradient colors.
Use a professional animated background, not too dark.
Suggested style:
- light blue / cyan / navy gradient
- slow moving radial gradient blobs
- clean scientific dashboard look

5. Keep the login card centered and professional.

HEADER / MAIN LAYOUT
1. Move the navigation sidebar from the right side to the left side.

2. In the main dashboard header:
- Add company logo on the right side.
- Add SQU logo on the left side.
- Create placeholder image paths:
  frontend/public/company-logo.png
  frontend/public/squ-logo.png

3. Replace header title:
SQU Weather Station
with:
Smart Weather Station

4. Make the header title centered.

5. Make the header slightly larger.

6. Use a comfortable blue color that is easy for the eyes.
The header should have a subtle animated blue gradient.

7. Keep system status online visible.

HOME DASHBOARD
1. Keep this description, but improve its UI:
"Sultan Qaboos University Weather Station
This dashboard monitors environmental and air quality data collected from multiple sensors connected to the DataTaker DT80W data logger. The system converts logged DBD files into CSV files and displays readings, trends, and downloadable reports."

2. Center this description section.

3. Make it more beautiful:
- colored elements
- soft blue/cyan background
- icon or logo
- glass card style
- professional spacing

4. In Sensor Status Overview cards:
- Do NOT show the main reading value like 41.3°C.
- Instead show:
  Last Record: 28 Apr 2026, 20:15:00
  Parameters: X
  Records: X
  Status: Active

5. Keep the View Details button.
The user should open the sensor detail page to see all actual readings.

FOOTER
1. Make footer design better with a good blue color.
2. Footer center text:
SQU Smart Weather Station Monitoring System © 2026 | Developed for environmental data monitoring

3. Add on the right side:
iLab Marine

4. Footer should be visible on all pages.
5. Footer should not cover table content.

SENSOR PAGES
1. Sensor pages must show only real CSV rows.
2. Data table row count must equal actual CSV row count.
3. Latest cards must use the last real CSV row.
4. Charts must use only real CSV rows.
5. Date filter must filter only real CSV data.
6. Excel export must export only real CSV data.

DOCUMENTATION
Update README and docs/FUTURE_DT80W_INTEGRATION.md:
- Explain that the app does not generate data.
- CSV files are the source of truth.
- To update data, replace the CSV files with the real files generated by the Python DBD converter.
- Sampling interval is currently saved locally only and real DT80W command integration will be added later.

After fixing, test:
1. Login works.
2. Home record counts match actual CSV rows.
3. Sensor tables show only actual CSV rows.
4. Excel export exports only actual CSV rows.
5. Sidebar is on the left.
6. Header/footer/logo placeholders are correct.
7. No TypeScript errors or API errors.